# Django settings
SECRET_KEY=your_very_secret_django_key_here
DEBUG=True

# Database settings (Example for MySQL)
DATABASE_ENGINE=django.db.backends.mysql
DATABASE_NAME=caspinex_db
DATABASE_USER=caspinex_user
DATABASE_PASSWORD=your_db_password
DATABASE_HOST=127.0.0.1
DATABASE_PORT=3306

# Database settings (Example for SQLite - if you prefer to start with this)
# DATABASE_ENGINE=django.db.backends.sqlite3
# DATABASE_NAME=db.sqlite3

# API settings (if any, e.g., external service keys)
# Example: SOME_EXTERNAL_API_KEY=

# Email settings (if configured)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your_email@example.com
# EMAIL_HOST_PASSWORD=your_email_password
